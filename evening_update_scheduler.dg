day = zoho.currentdate.toString("EEEE");
if(day == "Sunday")
{
	return;
}
msg = Map();
msg.put("text","🌆  Good Evening team!\n\nPlease update the tasks you have completed today .");
zoho.cliq.postToChannel("dailytaskst",msg);
